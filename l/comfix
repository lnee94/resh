
#bash-lib-o
id=("comfix" "2.0" "01e13ab1-f94a-4c11-8d4b-84e90200373c" )
defaltrepo="https://raw.githubusercontent.com/lnee94/resh/main/l"
###init-o
shopt -s expand_aliases
if [ "x$REPO" = "x" ] ;then REPO=$defaltrepo ;fi
depin="resher"
if [ "$(type -t resher)" != "function" ] ;then depinst(){ eval "$2""$(curl -s $REPO/$1)";depin="depinst" ;} ;fi
###init-c
###deps-o 
###deps-c
###code-o

case $arg in

  help)
    echo "adds mutal line commits start with +- end with __
note if you use allfix it will give a error mesage this is not bad 
allfix makes it so that all command do not run until __"
    ;;
    
  remove)
    unalias +-
    ;;
  allfix)
  alias \+\-=": <<__('"
  ;;

  *)
    alias \+\-=': <<__'
    ;;
esac

###code-c
###exit-o
unset arg
###exit-c
###bash-lib-c 
